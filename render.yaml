services:
  - type: web
    name: http-proxy-service
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DEBUG
        value: false
    healthCheckPath: /health
